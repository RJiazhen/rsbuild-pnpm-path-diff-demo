{
  "name": "rsbuild-pnpm-path-diff-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "build:no-minify": "rsbuild build --env-mode no-minify",
    "build:all": "pnpm build:no-minify && pnpm build",
    "build:all:watch": "pnpm build:no-minify --watch & pnpm build --watch",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "my-utils": "file:../my-utils"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.5.6",
    "@rsbuild/plugin-react": "^1.4.0",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "typescript": "^5.9.2"
  }
}
